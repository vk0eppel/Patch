# Rust
target/
*.pdb

# Flutter / Dart
patch_app/.dart_tool/
patch_app/build/
build/
patch_app/.flutter-plugins-dependencies

# Release artifacts written to the repo root by release.yml's packaging steps
# (also produced by local release testing).
Patch-*.dmg
Patch-*.exe

# macOS
.DS_Store
*.swp

# Stray patch.toml left in CWD by the pre-FFI dev binary.
# The runtime config now lives under dirs::data_dir() (e.g.
# ~/Library/Application Support/Patch/patch.toml on macOS).
patch.toml

# IDE
.idea/
.vscode/
*.iml
